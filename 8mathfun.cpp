#include <iostream>
#include <cmath>



int main() {

    double x = 3.14;
    double y = 4;
    double z;

    //z = std::max(x , y);
    //z = std::min(x, y);   
    //z = pow(2, 3);   //pow = power
    //z = sqrt(4);
    //z = abs(-3); //absolute power (jak daleko jest od zera)
    //z = round(x); //zaokrągla liczby
    //z = ceil(x); // zaokrągla do góry
    //z = floor(x); // zaokrągla na dół


// https://cplusplus.com/reference/cmath/ 


    std::cout << z;
    
    return 0;
} 